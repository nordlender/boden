---
// Placeholder — admins can't post moderator messages yet (tracked in a
// follow-up issue: the /admin authoring UI and storage don't exist). This
// renders a static empty-state box so the index page has the right layout
// and slot ready; swap the body for the real message list once that ships.
---

<div class="rounded-lg border border-border bg-surface p-4">
	<h2 class="text-sm font-semibold text-text">Messages</h2>
	<p class="mt-1 text-sm text-text-muted">No messages from admins right now.</p>
</div>

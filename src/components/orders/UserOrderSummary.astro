---
import OrderCodeHeader from './OrderCodeHeader.astro';
import OrderContents from './OrderContents.astro';

interface OrderItemView {
	itemName: string;
	requestedQuantity: number;
}

interface Props {
	orderCode: string;
	status: string;
	fromDate: string;
	toDate: string;
	note: string | null;
	items: OrderItemView[];
}

const { orderCode, status, fromDate, toDate, note, items } = Astro.props;
---

<section class="order-summary flex flex-col gap-4">
	<OrderCodeHeader orderCode={orderCode} status={status} fromDate={fromDate} toDate={toDate} />
	<OrderContents items={items} note={note} />
</section>

---
import AppIcon from '../icons/AppIcon.astro';

interface Props {
	name: string;
}

const { name } = Astro.props;
---
<span class="inline-flex max-w-full items-center gap-1 truncate rounded-full bg-bg-subtle px-2 py-0.5 text-xs text-text-muted" title={name}>
	<AppIcon name="product" size={14} class="shrink-0 text-text-muted" />
	<span class="truncate">{name}</span>
</span>

---
export const prerender = false;

// Coexists with review/[code].astro (same pattern as retrieve.astro
// coexisting with a retrieve/ directory elsewhere in this feature). A
// bare /moderator/review visit (no code) has nowhere useful to land but the
// requests queue.
return Astro.redirect('/moderator/requests');
---
